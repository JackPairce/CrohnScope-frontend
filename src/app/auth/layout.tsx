export const metadata = {
  title: "Authentication",
  description: "Authentication portal for the application",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
