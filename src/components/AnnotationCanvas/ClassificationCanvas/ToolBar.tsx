// Generated by Copilot
import { Dispatch, SetStateAction } from "react";
import type { State } from "./LayerState";

interface ClassificationToolbarProps {
  layerState: State;
  setLayerState: Dispatch<SetStateAction<State>>;
}

export default function ClassificationToolbar({
  layerState,
  setLayerState,
}: ClassificationToolbarProps) {
  return (
    <div className="layers-tools">
      <div className="layer-controls">
        <button
          className={layerState === "all" ? "active" : ""}
          onClick={() => setLayerState("all")}
        >
          All Regions
        </button>
        <button
          className={`healthy ${layerState === "healthy" ? "active" : ""}`}
          onClick={() => setLayerState("healthy")}
        >
          Healthy
        </button>
        <button
          className={`unhealthy ${layerState === "unhealthy" ? "active" : ""}`}
          onClick={() => setLayerState("unhealthy")}
        >
          Unhealthy
        </button>
      </div>
    </div>
  );
}
